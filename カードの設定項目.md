# 関数
* 関数書き方
    @関数名{
        ここにコマンド
    }

    @関数名
    {
        ここにコマンド
    }

    スペースはなくてもよい
    コマンドは1行に一個

* 既存関数
    * これらはカード作成に必須
        @初期設定
        @効果

    * 必要なら


# enum

* 発動タイミング
    ターン開始時
    ターン終了時
    手動発動
    場から離れた時
    破壊時

* 耐久値減少条件
    ターン終了時
    効果発動時

* 属性
    無属性
    光属性
    闇属性

* カードタイプ
    儀式
    建物

* 特性
    消滅
    崩壊
    臨時
    保護
    一時保護

    * それぞれの効果
        消滅:プレイした後除外
        崩壊:ターン終了時、手札にあるなら除外
        臨時:手札から離れたとき除外(プレイしたら発動してから除外)
        保護:ターン終了時、手札から離れない
        一時保護:ターン終了時、手札から離れない。1回発動したり、手札から離れたら消える


# 初期設定
    

* カード設定
    カード名:名前
    カードタイプ:カードタイプenum
    属性:属性enum
    カード説明:説明
    エネルギーコスト:属性enum, 数値　(無属性を入れると両方のエネルギー数値分使う。片方足らなくてもよい)
    カード特性:特性enum,…
    発動条件:関数名　※

    ※ 補足
    必要なら使う。returnでboolになるのを関数内で返す。returnなければfalse。
    この関数内で表向きするなどの画面でカードが動くタイプの効果コマンドを使っても何も起きない。
    true=発動できる,false=発動できない
    


* 建物設定
    耐久値:耐久値
    耐久値減少条件:耐久値減少条件enum
    発動タイミング:発動タイミングenum

* リソース
    画像:ファイルパス


# カード効果設定

* リソース系
    スコア加算:数値
    エネルギー加算:属性enum, 数値
    暴走ゲージ減少:数値

* 山札系
    表向きにする:枚数, 表向きにしたカード
    表向き選択:枚数, 最小枚数, 最大枚数, 選択したカード, 選択しなかったカード
    山札を見る:枚数, 見たカード
    山札を見る選択:枚数, 最小枚数, 最大枚数, 選択したカード, 選択しなかったカード
    山札下送り:送るカード(シャッフルされて送られる)
    山札選択:最小枚数, 最大枚数, 選択したカード
    
* 墓地系
    墓地送り:送るカード
    無公開墓地送り:送るカード
    墓地選択:最小枚数, 最大枚数, 選択したカード
    
* 手札系
    手札に加える:加えるカード
    手札選択:最小枚数, 最大枚数, 選択したカード

* フィールド系
    フィールド選択:最小枚数, 最大枚数, 選択したカード

* カード変数
    カード選択:カード, 最小枚数, 最大枚数, 選択したカード
    カードランダム選択:カード, 最小枚数, 最大枚数, 選択したカード

* カード変数フィルター(条件にあってるカードをフィルタされたカードに分離)
    属性フィルター:属性enum, カード, フィルタされたカード
    タイプフィルター:タイプenum, カード, フィルタされたカード
    名前フィルター:名前, カード, フィルタされたカード　※

    ※名前の文字が入っていればフィルタされる。
    * 例文
        名前:光の

        カード名:光の導き <-フィルタされる
        カード名:栄光 <-フィルタされない
        カード名:栄光の輝き <-フィルタされる
        カード名:光速のドロー <-フィルタされない

* 特性
    特性付与:カード, 特性enum

* 制御構文
    if
    return

    * if文書き方例(スペースはなくてもよい)
        if ($カード.属性 != 無属性) {
            ここにコマンド
        }

    returnは関数の強制終了や返したい値があるなら
    返したい値がある場合
        return, 返したい値
        bool式でも良い。スペースはなくてもよい



関数呼び出し:関数名

# 変数
* 変数書き方
    #変数名 <- int変数
    $変数名 <- カード変数

* 変数操作
    =
    +=
    -=
    *=
    /=
    -数値

    * 使い方
        #変数名 += 1
        #変数名,+=,#変数名
        #int-=#カード枚数+ 2

* カード変数の変数参照(最初に入れた先頭のカードが参照先になる)
    枚数(カード変数内の枚数のこと)
    名前
    属性
    タイプ
    耐久値
    
    * 書き方例
        $変数名.属性


* 数値参照(#無しで参照)
    光エネルギー
    闇エネルギー
    山札枚数
    墓地枚数
    手札枚数
    スコア

* カード参照($無しで参照)
    手札
    墓地
    山札
    
# サンプル

@初期設定{
    カードタイプ:儀式
    属性:光属性
    カード説明:説明
    エネルギーコスト:光属性, 2
}

@効果{
    表向きにする:3,$カード
    属性フィルター:光属性, $カード, $フィルター
    手札に加える:$フィルター
}