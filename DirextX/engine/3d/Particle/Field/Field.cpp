#include "Field.h"
#include "cassert"

Sphere Field::GetSphere()
{
    assert(false);
    return Sphere();
}

void Field::SetSphere(Sphere& sphere)
{
    assert(false);
    sphere;
}

AABB Field::GetAABB()
{
    assert(false);
    return AABB();
}

void Field::SetAABB(AABB& aabb)
{
    assert(false);
    aabb;
}
